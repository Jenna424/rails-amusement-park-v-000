<%= form_for @user, url: "/users/login" do |f| %>

  <div class="field">
    <%= f.label :name, "Select your name from the dropdown menu below:" %><br>
    <%= f.collection_select(:id, @users, :id, :name) %>
  </div>

  <div class="actions">
    <%= f.submit "Sign In" %>
  </div>
  
<% end %>
